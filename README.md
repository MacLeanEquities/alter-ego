# Alter Ego



Scaffolded by AI OS Production v1 from signed blueprint `2392e0d7-f2ba-411c-870e-3998ca860c14`
(template generic-tool-content/v2).

## Build
```
npm install
npm run build      # renders ui_spec pages to dist/*.html on the shared UI kit
```

Content in `content/` came through the content-writer (ContentEngine). The
strategic scope lives in the signed blueprint; this repo does not redefine it.
